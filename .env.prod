# Production Environment Configuration
# Copy this file and update with your actual production values

# Database Configuration
DATABASE_URL=postgresql://postgres:your-secure-password@db:5432/dealhub_crm
POSTGRES_DB=dealhub_crm
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters

# Email Configuration (for password reset, notifications)
EMAIL_USER=your-email@yourdomain.com
EMAIL_PASS=your-email-app-password

# Application Settings
NODE_ENV=production
PORT=3000

# Optional: External Database (if not using Docker PostgreSQL)
# DATABASE_URL=postgresql://username:password@your-db-host:5432/database_name
